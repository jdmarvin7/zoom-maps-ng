import { FormatarStringPipe } from './formatar-string.pipe';

describe('FormatarStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatarStringPipe();
    expect(pipe).toBeTruthy();
  });
});
